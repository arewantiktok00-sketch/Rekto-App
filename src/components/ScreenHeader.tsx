/**
 * Re-export standard screen header for RTL-safe layout.
 * Use on every screen: back (40) + centered title (flex:1) + spacer (40).
 */
export { ScreenHeader } from '@/components/common/ScreenHeader';
